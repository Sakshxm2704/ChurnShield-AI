# Screenshots

Place screenshots of the running platform here:

- `dashboard.png`   — Main KPI dashboard
- `prediction.png`  — Single prediction page
- `batch.png`       — Batch prediction results
- `analytics.png`   — Analytics charts page
- `shap.png`        — SHAP explainability
- `whatif.png`      — What-If simulator
- `monitoring.png`  — Platform monitoring dashboard
- `alerts.png`      — Alert log view

## How to capture

1. Start the platform: `python -m backend.app` + `python -m frontend.server`
2. Open http://localhost:8501
3. Take screenshots and save here with the filenames above
